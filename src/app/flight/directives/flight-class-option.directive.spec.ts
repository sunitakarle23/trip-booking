import { FlightClassOptionDirective } from './flight-class-option.directive';

describe('FlightClassOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightClassOptionDirective();
    expect(directive).toBeTruthy();
  });
});
